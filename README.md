# Spotidown
SpotiDown is a Windows application that lets you download individual tracks and entire playlists using Spotify links. It finds audio on YouTube, saves it as MP3, and automatically adds the song title, artist, album, and artwork from Spotify.  Artwork is embedded directly into each MP3 file, so there is no need to keep separate image
